@model QNews.Base.Url

@if (Model.ContentID.HasValue)
{
    ViewBag.Title = Model.Content.Title;
    <h1>@Model.Content.Title</h1>
    
    <div class="des">
        @Html.Raw(Model.Content.Description)
    </div>
    
    <div>
        @Html.Raw(Model.Content.Details)
    </div>
}